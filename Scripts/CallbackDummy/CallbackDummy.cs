using UnityEngine;
using System.Collections;

public class CallbackDummy : MonoBehaviour {
    //This script is responsible for relaying info between the server and the client
    //until a server is configured, this dummy will generate events automatically
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
